// Shared by client (lib/meta.ts) and server (app/api/meta) — no directive so
// both sides can import it. The ID is public by nature (visible in any browser).
export const META_PIXEL_ID = "1291157749527923";
