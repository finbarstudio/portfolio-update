/**
 * The finbar✶studio brand mark — the solid six-pointed star (U+2726 ✶).
 *
 * SVG polygon point list, normalised to a 0–100 viewBox and drawn point-up.
 * Single source of truth shared by the wordmark logo, favicon, app icons, OG
 * image and the loading spinner so the mark is identical everywhere.
 *
 * Outer radius 42, inner radius 11 (centre 50,50) — a tight inner radius keeps
 * the six spikes narrow and sharp (not the chunky Star-of-David look).
 */
export const STAR_POINTS =
  "50,8 55.5,40.5 86.4,29 61,50 86.4,71 55.5,59.5 50,92 44.5,59.5 13.6,71 39,50 13.6,29 44.5,40.5";
